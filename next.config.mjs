// /** @type {import('next').NextConfig} */

// const nextConfig = {};

// export default nextConfig;

// to run in docker:
const nextConfig = {
    output: 'standalone',
};

export default nextConfig;